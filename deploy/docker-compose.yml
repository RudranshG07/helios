services:
  helios:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    image: helios:latest
    env_file: ../.env
    environment:
      - PORT=8090
    ports:
      - "8090:8090"
    volumes:
      - helios-data:/app/data
    restart: always

volumes:
  helios-data:
